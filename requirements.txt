# Mirror Kaggle notebook Cell 2 — chỉ install những package KHÔNG có sẵn
# trong base image (gcr.io/deeplearning-platform-release/pytorch-gpu.2-1.py310).
# KHÔNG đụng torch/torchvision/numpy/pandas/scikit-learn (base image đã có,
# pin lại sẽ vỡ CUDA bindings).

omegaconf==2.3.0
pycocoevalcap
scikit-image
torchinfo
wandb
loralib==0.1.1
iterative-stratification
iopath
hi-ml-multimodal
timm==0.6.13
spacy
nltk==3.8.1
transformers==4.44.2

# peft pinned to commit dùng trong repo gốc
peft @ git+https://github.com/huggingface/peft.git@e536616888d51b453ed354a6f1e243fecb02ea08

# --- GCP migration tooling ---
papermill==2.5.0
python-dotenv==1.0.1
google-cloud-storage==2.14.0
jupyterlab==4.0.11
pytest==7.4.4
pyyaml==6.0.1
